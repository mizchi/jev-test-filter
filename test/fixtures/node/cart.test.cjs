const { describe, it } = require("node:test");

describe("Cart", () => {
  describe("applyDiscount", () => {
    it("clamps at zero", () => {});
    it("rounds half up", () => {});
  });
  it("totals", () => {});
});
