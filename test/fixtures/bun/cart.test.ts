import { describe, expect, test } from "bun:test";

describe("Cart", () => {
  test("totals", () => {
    expect(1 + 1).toBe(2);
  });

  test("empties", () => {
    expect([]).toHaveLength(0);
  });
});
